{
    "version": 2,
    "builds": [
        {
            "src": "app.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/v1/api/signup",
            "dest": "/app.ts"
        },
        {
            "src": "/v1/api/login",
            "dest": "/app.ts"
        }
        
    ]
}